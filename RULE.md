# RULE.md — 项目开发规则

本文件定义本项目**必须遵守**的开发规则，由 `CLAUDE.md` 通过 `@RULE.md` 引入，
对所有协作者（含 AI Agent）生效。规则冲突时，以本文件为准。

## R1 · 先计划，后写入

- 写入文件（新增 / 修改）前，先给出**计划**：目标、涉及文件、实现方案与影响面，经人工确认后再实施。
- **例外**：骨架 / 脚手架文件（空实现、占位桩、目录结构）可直接创建，无需逐一确认。
- 计划应聚焦"改什么、为什么"，避免实施后大幅返工。

## R2 · 测试覆盖

- 所有代码必须**同时**具备单元测试与集成测试，缺一不可。
- 单元测试置于 `tests/unit/`（不依赖浏览器 / 网络）；集成 / 端到端测试置于 `tests/e2e/`。
- 提交新功能或修复时须同步提交对应测试；**全部测试通过方可合并**。
- 运行方式见 `CLAUDE.md`「常用命令」。

### R2 配套约定（测试分层与标记）

- 单元测试用 `@pytest.mark.unit` 标记，置于 `tests/unit/`，**不依赖浏览器 / 网络**，作为**快速门禁**。
- 集成 / 端到端测试用 `@pytest.mark.e2e` 标记，置于 `tests/e2e/`，可依赖 Playwright 浏览器内核。
- **CI 分两阶段**：`unit`（门禁，先跑且必须全绿）→ `e2e`（`needs: unit`，通过后才跑，依赖浏览器）。
- marker 已在 `pyproject.toml` 注册；本地运行：`pytest -m unit`、`pytest -m e2e`。

## R3 · 临时方案（Workaround）管理

采用临时方案必须走完以下三步，**不可跳过**：

1. **实施前**：说明更优路径**为何不可行**（技术约束 / 成本 / 时机 / 依赖缺失），并经**人工确认**。
2. **实施中**：在代码中以 `TODO(workaround): <原因 + 替换方向>` 显式标记，便于后续检索与回收。
3. **实施后**：说明**残留风险**与**回收方式**（如何回退、如何替换为正式方案、影响哪些模块）。

> 原则：临时方案是欠债，必须可追踪、可回收，不得悄悄变成"永久方案"。

## R4 · 代码质量与文档

- 编写代码须兼顾三者：**可维护性 · 可读性 · 健壮性**。
- 每个**文件、类、函数**之前必须写明**具体作用**（文件头注释 / 类 docstring / 函数 docstring），说明"做什么、为什么、关键约束"。
- 处理边界条件与异常路径，**避免静默失败**；失败要么恢复、要么明确抛出并记录。

## R5 · 变更沉淀

### 适用前提

- **仅多轮推进的任务**需要文档沉淀；一次性的小任务（改文案、装依赖、小修小补等）**无需**专门沉淀。
- 文档**不是快照**：须随进展**同步更新**，始终反映最新状态——过时的文档比没有文档更有害。

### 提交要求

- 所有**大的修改**（架构调整、新增模块、规则变更、重要修复）必须提交到 Git，commit message 清晰说明"做了什么 + 为什么"。
- 文档与代码**同批提交**，避免"代码先行、文档缺失"。

### 沉淀文档必含五要素（模板见 `docs/session-doc-template.md`）

1. **当前目标**：本轮要达成什么。
2. **关键约束**：技术 / 时间 / 规则等限制条件。
3. **已达成结论**：已确定的决策与产出。
4. **待解决问题**：尚未闭环的事项与风险。
5. **下一步计划**：后续动作与顺序。

## R6 · 先收敛计划（计划模式纪律）

面对**复杂任务**，先收敛计划再动手。

### 适用场景（满足其一即应先规划）

- 任务**不清晰**，目标或边界尚不明确。
- 需求存在**多个分支或决策点**，需先取舍。
- 希望**先讨论方案、拆分步骤、确认边界**，再进入实现。

### 约束

- 任务**清晰明了**时，**不要**开启计划模式，避免拖累节奏。
- 计划模式**只负责制定计划**，不进行实际落地；落地在计划确认后另行执行。
- 与 R1 配合：R6 管"任务级——要不要先规划"，R1 管"文件级——写入前确认"。

## R7 · 文档双轨制（技术文档 + 学习文档）

> 来源：`doc-system-kit` 迁移底稿（TestPlatform `RULES.md` §18+§19）。技术文档（事实源）+ 学习文档（面试镜像）双轨并行。
> 沉淀要求（底稿 §A，与本章同源）已由 **R5 · 变更沉淀** 完整覆盖，不再重复；执行同步任务时 R5 与本章配合适用。
> 执行流水线细节见技能 `.claude/skills/doc-study-sync/SKILL.md`；本章为规则源头，SKILL 内嵌副本与之冲突时以本章为准。

### R7.0 顶层铁律（AI 行为基石）

1. **事实源唯一性**：`docs/` 是绝对真理。学习文档是渲染视图：只允许改变表达形式；严禁改变数值、路径、键名。
2. **原子块保护**：正则 `[a-zA-Z0-9_\-/.]+` 匹配的连续串（路径、环境变量、函数名、端点等）视为不可分割原子。改写时原样保留；禁止在原子中间加空格、换行或「的」。

### R7.1 技术文档准则（源文档 = 考古挖掘）

目标：像严谨的代码考古学家，只记录，不发挥。源文档：`docs/architecture.md`、`docs/roadmap.md`、`README.md`。

| # | 准则 | AI 执行逻辑 |
|---|---|---|
| 1 | 零幻觉提取 | 提及配置键、端点时，必须附代码仓库精确引用（行号或模块路径）；不确定的数值写 `[待确认: xxx]`，禁止编造 |
| 2 | 为何优先于如何 | 每个模块只解释 1 个核心设计动机（解决什么业务痛点）；禁止逐行翻译源码逻辑 |
| 3 | 变更联动触发 | 代码合入而源文档未同步更新时，必须输出 `⚠️ 文档未同步` 拦截警告 |
| 4 | 复杂图式降维 | 状态机等复杂逻辑，强制输出 Mermaid 图代码（替代纯 ASCII），利于镜像端渲染 |

### R7.2 学习文档准则（镜像 = 翻译官）

目标：像资深培训师，把天书译成睡前故事。

| # | 准则 | AI 执行逻辑 |
|---|---|---|
| 1 | 断句切分（硬约束） | 以 。！？； 分句。中文自然句超 25 字，在中间用「（即...）」插入口语化补丁；连续 3 句超 20 字，必须插入一句「你可能会问...」打破节奏 |
| 2 | 「的」字死刑（硬约束） | 删除结构助词「的」（x的y → xy，如「任务的ID」→「任务 ID」）；保留「的确」「目的」等非结构助词 |
| 3 | 被动语态猎杀 | 「被」一律改写为主动句；例外仅固定术语（如被测接口/被测系统，按项目术语表） |
| 4 | 动词复活术 | 「进行/实施/开展+名词」直接换为动词（「进行删除」→「删除」） |
| 5 | 虚词过滤库 | 自动删除：众所周知、显而易见、总的来说、需要注意的是、基本上 |
| 6 | 类比四象限锚定（核心） | 类比必须且仅选自四象限：厨房（幂等=微波炉热剩饭，热几次都一样）、交通（限流=收费站车道数）、快递（消息队列=快递柜暂存）、人体（缓存=短期记忆）。严禁技术术语解释技术术语（如「分布式=多节点协同」直接红牌） |
| 7 | 面试加餐（红线） | 每章节结尾强制输出「面试官追问」：基于本段内容，提 1 个可能深挖的问题 |
| 8 | 豁免区 | 命令、表格、Mermaid 图、链接原封不动搬运；不参与字数与「的」字检查 |
| 9 | 事实一致性三重校验 | 写完后必须回查：①端点路径 ②配置键名 ③数字阈值——必须与源文档逐字相同 |

### R7.3 同步 SOP（强制流水线，按序执行）

1. **Diff 识别**：对比源文档最新 commit，标记变更段落（新增/修改/删除）。
2. **冲突熔断**：镜像数值与源冲突时，禁止自行计算或折中；必须输出 `❌ [数据冲突待人工仲裁: 源值X vs 镜像值Y]` 并终止本次同步。
3. **类比复用与刷新**：源变更仅改措辞未改事实 → 保留镜像原有类比；事实变更 → 废弃旧类比，按 R7.2-6 四象限法重新生成。
4. **版本印记**：学习文档头部自动更新为 `源版本: [commit hash] | 同步日期: [UTC 时间]`。

### R7.4 仓库与路径

- 学习文档远端仓库：`https://github.com/Testamentt/docs_study.git`（多项目共用，顶层文件夹区分项目）。
- 本地工作副本：`E:\Project\AutoAiSelfHeal-docs-study`（独立 git 仓库）；本项目内容在 `AutoAiSelfHeal/` 子目录，路径与源文档一一镜像。
- 镜像仓库与源代码仓库**完全隔离**：源仓库不提交它、不构建它、不引用它。
- 新增或修改技术文档时，按 R7.3 SOP 同批更新镜像并推送；只改一处视为任务未完成。
- `docs/sessions`、`docs/plans`、`docs/reviews`、`docs/TODO.md`、`docs/session-doc-template.md` 属沉淀/历史记录，不参与双轨。
- 内容冲突以技术文档为准：先改源，再同步镜像，禁止只在镜像处修 bug。
